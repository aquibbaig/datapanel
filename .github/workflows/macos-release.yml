name: macOS Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build macOS .app
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.4"
          cache: true

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.12.0

      - name: Build Datapanel.app
        run: ./scripts/build-mac-app.sh

      - name: Package app bundle
        id: package
        run: |
          set -euo pipefail
          mkdir -p build/dist
          short_sha="${GITHUB_SHA::7}"
          zip_path="build/dist/Datapanel-macOS-${short_sha}.zip"
          ditto -c -k --keepParent build/bin/datapanel.app "$zip_path"
          echo "zip_path=$zip_path" >> "$GITHUB_OUTPUT"
          echo "tag=macos-${short_sha}" >> "$GITHUB_OUTPUT"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Datapanel-macOS
          path: ${{ steps.package.outputs.zip_path }}
          if-no-files-found: error

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ steps.package.outputs.tag }}
          ZIP_PATH: ${{ steps.package.outputs.zip_path }}
        run: |
          set -euo pipefail
          gh release create "$TAG_NAME" "$ZIP_PATH" \
            --target "$GITHUB_SHA" \
            --title "Datapanel macOS ${GITHUB_SHA::7}" \
            --notes "Automated macOS app build from main at ${GITHUB_SHA}."
